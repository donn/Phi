namespace RiscBEE {
    module Extender<inputLength>(isSigned: Input, input: Input[inputLength - 1..0], out: Output[31..0]) {
        out = [(32 - inputLength)[[isSigned ? input[inputLength - 1]: 1b0]], input[inputLength - 1..0]];
    }
}