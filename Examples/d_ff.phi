module dFlipFlop(
    a: Input[31..0],
    b: Output[31..0],
    clock: Input,
    reset: Input
) {
    Wire[31..0] v = x;
    Register[31..0] store = 32b0;

    store.clock = clock;
    store.reset = reset;
    store.next = a;

    b = store;
}