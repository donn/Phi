
        integer description { $$ = $1; $$->right = $2; head = $$; }
        | { $$ = NULL; }
        ;




integer:
        NUMERIC {
            $$ = makeNode(NODE_TYPE_INT, &(struct Int){NODE_INT_DEFAULT_WIDTH,$1}, NULL, NULL);
        }
        ;