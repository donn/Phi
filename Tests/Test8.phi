module Counter(
    clock: @clock Input,
    reset: @reset Input,
    enable: @enable Input,
    output: Output[31..0]
) {
    Register[31..0] counter = 32b0
    counter.reset = reset
    counter.data = counter &+ 32b1
    counter.enable = enable

    output = counter
}